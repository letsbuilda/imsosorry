name: "GitHub Pages - Python Sphinx"

on:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    uses: darbiadev/.github/.github/workflows/github-pages-python-sphinx.yaml@c4fb123ed14ed8cc9df9820ca63bace02981e9fe # v1.0.0
